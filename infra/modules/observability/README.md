# module: observability

Creates the Log Analytics workspace that the Container Apps environment streams
logs to. Outputs the resource `id` (consumed by the
`container-app-environment` module) and the `workspace_id` GUID.
